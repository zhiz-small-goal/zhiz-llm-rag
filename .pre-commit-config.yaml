repos:
  - repo: local
    hooks:
      - id: rag-gate-fast
        name: rag-gate --profile fast
        entry: python tools/gate.py --profile fast --root .
        language: system
        pass_filenames: false
